services:
  - type: web
    name: tecaigo-pitch-2026
    runtime: node
    plan: free
    branch: main
    buildCommand: npm ci && npx next build --webpack
    startCommand: npx next start -H 0.0.0.0 -p $PORT
    healthCheckPath: /
    autoDeployTrigger: commit
    renderSubdomainPolicy: enabled
